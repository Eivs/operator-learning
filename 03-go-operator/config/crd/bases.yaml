# 此文件由 controller-gen 从 Go 类型定义自动生成
# 命令: controller-gen crd paths=./api/v1 output:crd:artifacts:config=config/crd
#
# 你也可以直接使用 01-crd-basics/appservice-crd.yaml
# 本文件通过 kubebuilder markers 生成，包含更完整的 Schema
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: appservices.example.com
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    app.kubernetes.io/part-of: appservice-operator
spec:
  group: example.com
  names:
    kind: AppService
    listKind: AppServiceList
    plural: appservices
    shortNames:
      - as
    singular: appservice
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          description: AppService 是 appservice 自定义资源的 Schema。它代表一个由 Operator 管理的 Web 应用程序。
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              type: object
              description: Spec 定义期望状态。
              required:
                - image
                - replicas
              properties:
                image:
                  type: string
                  description: Image 是容器镜像地址，必填字段。
                replicas:
                  type: integer
                  description: Replicas 是期望的 Pod 副本数量。
                  minimum: 1
                  default: 1
                port:
                  type: integer
                  description: Port 是服务暴露的端口号。
                  minimum: 1
                  maximum: 65535
                  default: 80
                resources:
                  type: object
                  description: Resources 是可选的容器资源限制。
                  properties:
                    cpu:
                      type: string
                      description: CPU 请求量，如 "100m" 或 "1"。
                    memory:
                      type: string
                      description: Memory 请求量，如 "128Mi" 或 "1Gi"。
                env:
                  type: array
                  description: Env 是可选的环境变量列表。
                  items:
                    type: object
                    required:
                      - name
                    properties:
                      name:
                        type: string
                        description: Name 是环境变量名。
                      value:
                        type: string
                        description: Value 是环境变量值。
                      valueFrom:
                        type: object
                        description: ValueFrom 从其他来源获取值。
                        properties:
                          configMapKeyRef:
                            type: object
                            required:
                              - name
                              - key
                            properties:
                              name:
                                type: string
                              key:
                                type: string
                          secretKeyRef:
                            type: object
                            required:
                              - name
                              - key
                            properties:
                              name:
                                type: string
                              key:
                                type: string
            status:
              type: object
              description: Status 定义当前状态，由 Operator 维护。
              properties:
                availableReplicas:
                  type: integer
                  description: AvailableReplicas 是当前可用的 Pod 副本数。
                conditions:
                  type: array
                  description: Conditions 表示资源的当前状态条件。遵循 Kubernetes API 约定。
                  items:
                    type: object
                    required:
                      - type
                      - status
                    properties:
                      type:
                        type: string
                      status:
                        type: string
                        enum:
                          - "True"
                          - "False"
                          - Unknown
                      lastTransitionTime:
                        type: string
                        format: date-time
                      reason:
                        type: string
                      message:
                        type: string
                deploymentName:
                  type: string
                  description: DeploymentName 是关联的 Deployment 名称。
                serviceName:
                  type: string
                  description: ServiceName 是关联的 Service 名称。
      additionalPrinterColumns:
        - name: Image
          type: string
          description: 容器镜像
          jsonPath: .spec.image
        - name: Replicas
          type: integer
          description: 期望副本数
          jsonPath: .spec.replicas
        - name: Port
          type: integer
          description: 服务端口
          jsonPath: .spec.port
        - name: Available
          type: integer
          description: 可用副本数
          jsonPath: .status.availableReplicas
        - name: Age
          type: date
          jsonPath: .metadata.creationTimestamp
